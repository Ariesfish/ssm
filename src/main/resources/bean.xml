<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/aop
        https://www.springframework.org/schema/aop/spring-aop.xsd">

    <bean id="accountService" class="xyz.ariesfish.service.AccountServiceImpl">
        <!-- inject DAO -->
        <property name="accountDao" ref="accountDao"></property>
    </bean>

    <bean id="accountDao" class="xyz.ariesfish.dao.AccountDaoImpl">
        <!-- inject QueryRunner -->
        <property name="runner" ref="runner"></property>
        <!-- inject ConnectionUtils -->
        <property name="connectionUtils" ref="connectionUtils"></property>
    </bean>

    <!-- not use singleton runner -->
    <bean id="runner" class="org.apache.commons.dbutils.QueryRunner" scope="prototype">
        <!-- no set method, using constructor injection -->
        <constructor-arg name="ds" ref="dataSource"></constructor-arg>
    </bean>

    <!-- Configure Data Source -->
    <bean id="dataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource">
        <!-- inject infos for creating data source -->
        <property name="driverClass" value="com.mysql.jdbc.Driver"></property>
        <property name="jdbcUrl" value="jdbc:mysql://localhost:3306/spring"></property>
        <property name="user" value="root"></property>
        <property name="password" value="suzhou123"></property>
    </bean>

    <!-- Configure ConnectionUtils -->
    <bean id="connectionUtils" class="xyz.ariesfish.utils.ConnectionUtils">
        <!-- inject data source -->
        <property name="dataSource" ref="dataSource"></property>
    </bean>

    <!-- Configure TransactionManager -->
    <bean id="txManager" class="xyz.ariesfish.utils.TransactionManager">
        <property name="connectionUtils" ref="connectionUtils"></property>
    </bean>

    <!-- Configure AOP -->
    <aop:config>
        <aop:pointcut id="pt" expression="execution(* xyz.ariesfish.service.*.*(..))"/>
        <aop:aspect id="txAdvice" ref="txManager">
            <aop:before method="beginTransaction" pointcut-ref="pt"></aop:before>
            <aop:after-returning method="commit" pointcut-ref="pt"></aop:after-returning>
            <aop:after-throwing method="rollback" pointcut-ref="pt"></aop:after-throwing>
            <aop:after method="release" pointcut-ref="pt"></aop:after>
        </aop:aspect>
    </aop:config>
</beans>